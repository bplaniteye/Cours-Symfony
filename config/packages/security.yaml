security:
    encoders:        
        # App\Entity\Utilisateurs:
        #     algorithm: auto
        App\Entity\Auteurs:
            algorithm: auto
    # https://symfony.com/doc/current/security.html#where-do-users-come-from-user-providers
    providers:
        # used to reload user from session & other features (e.g. switch_user)
        # utilisateurs_provider:
            # entity: 
            #     class : App\Entity\Utilidateurs
            #     property: email
        auteurs_provider:        
            entity:    
                class: App\Entity\Auteurs
                property: email
        # used to reload user from session & other features (e.g. switch_user)
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
        main:
            anonymous: lazy
            # provider: utilisateurs_provider           
            # guard:
            #     authenticators:                                   
            #         - App\Security\UtilisateursAuthenticator               
            # logout:
            #     path: app_logout             

            provider: auteurs_provider           
            guard:
                authenticators:                                    
                    - App\Security\AuteursAuthenticator
     
            logout:
                path: app_logout    
                # where to redirect after logout
                # target: app_any_route 

            # activate different ways to authenticate
            # https://symfony.com/doc/current/security.html#firewalls-authentication

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true
    role_hierarchy:
        ROLE_EDITOR: ROLE_USER
        ROLE_ADMIN: ROLE_EDITOR
        ROLE_SUPER_ADMIN: ROLE_ADMIN        

    # Easy way to control access for large sections of your site
    # Note: Only the *first* access control that matches will be used
    access_control:
        # - { path: ^/admin, roles: ROLE_ADMIN }
        # - { path: ^/profile, roles: ROLE_USER }
