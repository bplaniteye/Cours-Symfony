{% extends 'base.html.twig' %}

{% block title %}Nouvel utilisateur{% endblock %}

{% block body %}
    <h1>Nouvel utilisateur</h1>
 <table class="table table-striped" >
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prénom</th>
               <!-- <th>Date de naissance</th> -->
                <th>Adresse</th>
                <th>Email</th>
                <th>Login</th>
                <th>Rôle</th>
               
            </tr>
        </thead>
        <tbody>
         {
            <tr>           
                <td>{{ utilisateurs.id }}</td>
                <td>{{ utilisateurs.nom }}</td>
                <td>{{ utilisateurs.prenom }}</td>
              <!--  <td>{#{ utilisateurs.date_de_naissance| date("d/m/Y") }#}</td> -->
                <td>{{ utilisateurs.adresse }}</td>
                <td>{{ utilisateurs.email }}</td>
                <td>{{ utilisateurs.login }}</td>
                <td>{{ utilisateurs.role }}</td>  
<div class="flex gap-2">
    <div class="h-32 w-32 border-box bg-red-500 animate-pulse text-white md:hidden" > {{ utilisateurs.id }} </div>
    <div class="h-32 w-32 border-box bg-yellow-500 animate-bounce text-white"> {{ utilisateurs.nom }} </div>
    <div class="h-32 w-32 border-box bg-blue-500 animate-ping text-white"> {{ utilisateurs.prenom }} </div>
    <div class="h-32 w-32 border-box radial-stripes-2 animate-spin text-white"> {{utilisateurs.adresse }} </div>
    <div class="h-32 w-32 border-box bg-black-500 animate-bounce text-white"> {{utilisateurs.role }} </div>                       
    <div class="h-32 w-32 border-box bg-purple-500 animate-bounce text-white"> {{utilisateurs.role }} </div>                       
          </div>>
           
        </tbody>
    </table>
        
     
{% endblock %}
