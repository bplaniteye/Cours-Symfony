{% extends 'base.html.twig' %}

{% block title %}categorie{% endblock %}

{% block body %}
    <h1>Categorie index</h1>
 <table class="table table-striped" >
        <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Résumé</th>
                <th>Action</th>
             
            </tr>
        </thead>
        <tbody>
         {# https://twig.symfony.com/doc/3.x/tags/for.html #}
        {% for cat in categorie %}
            <tr>           
                <td>{{ cat.id }}</td>
                <td>{{ cat.titre }}</td>
                <td>{{ cat.resume }}</td>
                     <td>
                <button class="bouton">
      <a class="lien" href="{{ path('index_showcategorie',{ 'id':cat.id }) }}"> Afficher </a> </button>
                </td>            
            </tr>
             {% endfor %}
        </tbody>
    </table>
        
     
{% endblock %}
